# Claude Code Notes

Follow `AGENTS.md` first. This file exists so Claude Code can find the same operating contract quickly.

Important constraints:

- Read `task.md`, `config.md`, and relevant `specs/` before making changes.
- Treat `lib/geo-presentation.typ` as the public API.
- Do not duplicate entire templates for AGU, EGU, CN, EN, or group meetings.
- Implement reusable behavior through presets, recipes, and semantic components.
- Run the relevant compile command before reporting completion.

