# Feature 0001 Acceptance

Run:

```bash
scripts/agent-smoke-test.sh
```

Expected:

- `main.typ` compiles.
- All `examples/*.typ` files compile.
- No example imports directly from `style/` or `slides/`.

